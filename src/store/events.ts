import { createAction } from '@reduxjs/toolkit';

export const resetNotification = createAction('events/resetNotifications');

export const someCombinedEvent = createAction('events/someCombinedEvent');
